@using FeesPackage.Data_Access;
@model List<tblAttorneyCombo>

@{
    ViewBag.Title = "tblAttorneyCombo";
}

<div class="span9">
    <div class="content">
        <div class="module">
            <div class="module-head">
                <h3>
                    <i>@ViewBag.Title</i> Table
                </h3>
            </div>
            <div class="module-body table">
                <table cellpadding="0" cellspacing="0" border="0" class="datatable-1 table table-bordered table-striped	display" width="100%">
                    <thead>
                        <tr>
                            <th>@Html.DisplayNameFor(modelItem => Model[0].Deposit_Options)</th>
                            <th>@Html.DisplayNameFor(modelItem => Model[0].Attorney_Combinations)</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (tblAttorneyCombo model in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => model.Deposit_Options)</td>
                                <td>@Html.DisplayFor(modelItem => model.Attorney_Combinations)</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>@Html.DisplayNameFor(modelItem => Model[0].Deposit_Options)</th>
                            <th>@Html.DisplayNameFor(modelItem => Model[0].Attorney_Combinations)</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
        <!--/.module-->
    </div>
    <!--/.content-->
</div>
<!--/.span9-->
