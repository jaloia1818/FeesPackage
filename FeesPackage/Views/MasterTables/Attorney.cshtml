@using FeesPackage.Data_Access;
@model List<tblAttorney>

@{
    ViewBag.Title = "tblAttorney";
}

<div class="span9">
    <div class="content">
        <div class="module">
            <div class="module-head">
                <h3>
                    <i>@ViewBag.Title</i> Table
                </h3>
            </div>
            <div class="module-body table">
                <table cellpadding="0" cellspacing="0" border="0" class="datatable-1 table table-bordered table-striped	display" width="100%">
                    <thead>
                        <tr>
                            <th>@Html.DisplayNameFor(modelItem => Model[0].Atty_Name)</th>
                            <th>@Html.DisplayNameFor(modelItem => Model[0].Atty_Initials)</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (tblAttorney model in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => model.Atty_Name)</td>
                                <td>@Html.DisplayFor(modelItem => model.Atty_Initials)</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>@Html.DisplayNameFor(modelItem => Model[0].Atty_Name)</th>
                            <th>@Html.DisplayNameFor(modelItem => Model[0].Atty_Initials)</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
        <!--/.module-->
    </div>
    <!--/.content-->
</div>
<!--/.span9-->
